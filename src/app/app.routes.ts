import { Routes } from '@angular/router';
import { PeriodicTable } from './features/periodic-table/periodic-table';

export const routes: Routes = [
    {path:"",component:PeriodicTable,title:"ChemFlow - Periodic table"}
];
